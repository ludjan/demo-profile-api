name: Deploy
on: 
  workflow_dispatch:
    inputs:
      environment:
        type: environment
    
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: "Deploy to ${{ GITHUB.ENVIRONMENT }}"
        run: "echo Deployed to production"
  
